# 会话上下文记录

**保存时间:** 2026-06-09 10:52:04
**启动目录:** E:\nrf_sample_codes\working_team_work\public\project\设计输入文档生成
**保存路径:** E:\nrf_sample_codes\working_team_work\public\project\设计输入文档生成\docs\20260609_105204_context.md

## 对话详情

### 背景

本会话是上一会话的延续（上一会话因上下文窗口溢出而压缩）。上一会话完成了设计输入文档Agent改造项目Phase 1的核心实施工作：

**上一会话完成的工作：**
- PoC验证：DeepSeek-V4-Pro工具调用兼容性（3/3测试通过）
- 4个新服务模块：agent_state.py, agent_prompt.py, agent_tools.py, context_manager.py
- 核心引擎：agent_engine.py（LangGraph StateGraph + HITL interrupt + SSE流式输出）
- 修改现有文件：minimax.py（新增_call_minimax_api_raw），routes.py（新增3个Agent SSE端点），main.py（新增lifespan + /agent路由）
- 前端：agent.html（~450行，聊天界面 + 侧边栏进度面板）

**上一会话遗留任务（Task #8 in_progress）：**
- 测试文件未编写
- requirements.txt未更新
- 系统启动验证未进行

### 本会话完成的工作

#### 1. 编写单元测试文件 tests/test_agent.py（31个测试用例）

覆盖范围：
- **TestAgentState（3个）：** 初始状态结构验证、状态快照构建、状态变更反映
- **TestAgentTools（5个）：** search_kb空结果/有结果格式、generate_section/revise_section错误处理、工具列表验证
- **TestAgentPrompt（4个）：** System Prompt包含5个段落、状态JSON注入、工具描述包含、灵活性原则包含
- **TestContextManager（6个）：** token估算、压缩阈值判断、消息数不足保护、降级摘要JSON有效性
- **TestSSEEventStream（6个）：** token/tool_start/tool_end/waiting_approval/done/error事件格式
- **TestHITLDecision（4个）：** approve/reject/edit决策解析
- **TestStateSerialization（2个）：** 初始状态和完整状态的JSON序列化/反序列化

#### 2. 编写评估脚本 tests/test_prompt_eval.py

- 10组场景定义（S1-S10）：覆盖标准SOP流、跳过处理、跨步骤跳转、模糊输入引导、HITL确认、知识库检索触发、拒绝编造、错误恢复、上下文保持、回复长度控制
- 评分函数：基于预期行为/禁止行为的命中率计算得分
- System Prompt静态覆盖率分析：用正则匹配检查prompt对各场景的覆盖
- MockAgentEvaluator：模拟理想Agent行为的评估框架（供后续E2E测试参考）

#### 3. 修复依赖问题

- **agent_state.py 第12行：** `langgraph.checkpoint.aiosqlite` → `langgraph.checkpoint.sqlite.aio`（适配langgraph-checkpoint-sqlite 3.1.0的路径变更）
- **agent_engine.py 第21行：** 同上修复
- **requirements.txt：** 新增 langgraph>=0.2.0, langgraph-checkpoint-sqlite>=2.0.0, langchain-openai>=0.1.0, langchain-core>=0.2.0, aiofiles>=23.0.0, pytest-asyncio>=0.23.0
- **安装pytest-asyncio**（之前遗漏的依赖）

#### 4. 创建测试配置文件

- **pytest.ini：** `asyncio_mode = auto`, `testpaths = tests`
- **tests/conftest.py：** pytest配置 + 项目路径注入

#### 5. 测试结果

- 单元测试：**31/31 通过**（0.64秒）
- 模拟评估得分：**96.7%**（9个A, 1个B）
- 导入链验证：全部通过（3个工具、3742字符prompt、10个状态分区、Graph构建成功）

### 项目最终状态

**新增文件（Phase 1）：**
| 文件 | 用途 |
|------|------|
| app/services/agent_state.py | DesignInputAgentState TypedDict + SqliteSaver管理 |
| app/services/agent_prompt.py | 5段式System Prompt构建（角色+SOP+工具+风格+状态快照） |
| app/services/agent_tools.py | 3个LangChain Tool：search_kb, generate_section, revise_section |
| app/services/context_manager.py | 滑动窗口上下文压缩（85%阈值，15轮保留） |
| app/services/agent_engine.py | LangGraph StateGraph核心引擎（Agent循环+HITL+SSE） |
| app/static/agent.html | Agent对话界面（聊天区+侧边栏进度面板，SSE流式消费） |
| tests/test_agent.py | 31个单元/集成测试 |
| tests/test_prompt_eval.py | 10场景评估脚本 |
| tests/conftest.py | 测试配置 |
| pytest.ini | Pytest配置 |

**修改文件：**
| 文件 | 变更 |
|------|------|
| app/services/minimax.py | 新增 `_call_minimax_api_raw()` 模块级函数 |
| app/api/routes.py | 新增3个Agent端点（messages/SSE, resume/SSE, state） |
| app/main.py | 新增lifespan（agent初始化/清理）、/agent路由、版本升至2.0.0 |
| requirements.txt | 新增langgraph系列依赖 |

**架构特点：**
- 完全向后兼容：旧 `/api/generate` 端点保持不变
- LangGraph-only架构（非DeepAgents）
- HITL通过 `interrupt()` + `Command(resume=...)` 实现
- SSE流式输出通过 `astream_events()` v2
- SqliteSaver持久化检查点

## 关键信息

- 项目目录：`E:\nrf_sample_codes\working_team_work\public\project\设计输入文档生成`
- LLM：DeepSeek-V4-Pro via langchain-openai ChatOpenAI（兼容模式）
- API Base URL：`https://ark.cn-beijing.volces.com/api/coding/v3`
- Python环境：conda env_01
- 测试框架：pytest + pytest-asyncio
- 知识库：贴敷式胰岛素泵，778文档，ChromaDB

## 决策记录

- **LangGraph-only架构选择**：用户在上一个会话中明确选择LangGraph-only（非DeepAgents），理由是依赖风险更低
- **langgraph checkpoint路径适配**：langgraph-checkpoint-sqlite 3.1.0改变了import路径，从 `langgraph.checkpoint.aiosqlite` 变为 `langgraph.checkpoint.sqlite.aio`
- **测试mock策略**：工具函数内部import（非模块级import），因此mock需target源模块（`app.services.rag.vector_store.VectorStore`、`app.services.minimax._call_minimax_api_raw`），而非importing模块
- **Phase 1工具范围**：用户选择3个核心工具（search_kb, generate_section, revise_section），Phase 2再扩展
