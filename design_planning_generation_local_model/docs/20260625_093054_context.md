# 会话上下文记录

**保存时间:** 2026-06-25 09:30:54
**启动目录:** E:\nrf_sample_codes\working_team_work\public\project\design_planning_generation_local_model
**保存路径:** E:\nrf_sample_codes\working_team_work\public\project\design_planning_generation_local_model\docs\20260625_093054_context.md

## 对话详情

### 用户:
（从上一会话恢复）我上传了附件，但是显示附件列表为空

### AI:
定位到前端 `agent.html` 中三个问题：

1. **PROJECT_ID 未持久化到 URL**：新项目生成的 PROJECT_ID 不写入地址栏，刷新页面后丢失
2. **`fetchAttachments()` 仅在已有项目时调用**：新项目不会从服务端同步附件列表
3. **Agent 交互后未同步附件**：`sendMessage()`、`resumeAgent()`、`startAutoGenerate()` 的 finally 块只调了 `fetchState()` 不调 `fetchAttachments()`

修改了 `app/static/agent.html`：
- 新项目自动 `history.replaceState` 写入 `?project=PROJECT_ID`
- `fetchAttachments()` 移到 `if (existingProject)` 外部，所有项目都加载
- `sendMessage`、`resumeAgent`、`startAutoGenerate` 的 finally 块增加 `fetchAttachments()`

### 用户:
继续吧

### AI:
继续深入排查，发现核心 bug 在后端 `routes.py` 的 `/agent/projects/{project_id}/messages` 端点。

**根因**：每次发消息都传入 `create_initial_state()`（`attachments=[]`），因为 `AgentState` 中 `attachments` 字段没有定义 reducer（默认替换），每次对话都把已保存的附件列表覆盖为空。

**修复 `app/api/routes.py`**：
- `/messages` 端点：发消息前先 `aget_state` 检查是否已有 checkpoint，有则不传 `initial_state`
- `/auto-generate` 端点：一键生成前保留已有附件到 `initial_state["attachments"]`

验证通过：附件在发消息后保持不变。

### 用户:
上传的附件内容能全部获取到吗

### AI:
分析了附件内容从上传到检索的完整链路，发现三个截断点：
- `MAX_TEXT_IN_STATE = 50000` 字符（state 存储截断）
- `MAX_CHUNKS_PER_DOC = 80`（向量库截断，≈12万字符）
- Embedder 单条 8000 字符/chunk（不触发）

`search_attachment` 工具双路径：TF 关键词匹配（在 state full_text 中）→ 回退向量检索。

中文 TF 匹配用 `str.split()` 无法分词 CJK，等于全文子串查找。

### 用户:
我是说上传的附件内容可以直接传给大模型吗

### AI:
当前不能。System Prompt 只注入附件摘要（文件名+字符数），不含 `full_text`。模型需主动调用 `search_attachment` 工具。

`qwen3.5:122b` 有 256K context，直接传入可行。

### 用户:
好的

### AI:
修改 `agent_prompt.py`：附件 `full_text` 直接追加到 System Prompt，模型无需工具调用即可阅读全部内容。

### 用户:
如何让大模型可以看到附件文档中完整的内容，包含文档的目录结构

### AI:
三个修改：

1. **`app/services/attachment_service.py`** — `_do_extract()` 合并段落时保留章节标题为 Markdown `## 标题`，同时生成 `toc` 目录
2. **`app/api/routes.py`** — `MAX_TEXT_IN_STATE` 从 50K 提升到 150K，附件数据新增 `toc` 字段
3. **`app/services/agent_prompt.py`** — System Prompt 中每个附件展示目录结构 + 全文内容

验证通过。

### 用户:
（save-context 命令）

## 关键信息

- **项目路径**: `E:\nrf_sample_codes\working_team_work\public\project\design_planning_generation_local_model`
- **项目类型**: QMS医疗器械文档生成系统（支持16种文档类型）
- **LLM 模型**: qwen3.5:122b 运行在 localhost:11435，Ollama 部署
- **嵌入模型**: qwen3-embedding:4b 运行在 localhost:11435，1024 维
- **qwen3.5:122b context window**: 256K（`qwen35moe` 架构，125.1B 参数，Q4_K_M 量化）
- **LangGraph 版本**: 1.2.5，使用 `AsyncSqliteSaver` 做 checkpoint 持久化
- **技术栈**: FastAPI + LangGraph Agent + ChromaDB + Ollama

## 决策记录

1. **附件列表为空 bug — 双层修复**：
   - 后端根因：`/messages` 端点每次传入 `create_initial_state()` 覆盖已有状态
   - 前端辅助：PROJECT_ID URL 持久化 + 所有场景同步附件
   
2. **附件全文注入 System Prompt**：
   - 利用 256K context 直接传入附件全文，模型无需工具调用
   - `MAX_TEXT_IN_STATE` 从 50K → 150K（预留 context 给对话历史）
   
3. **文档结构保留**：
   - `.docx` 提取时保留 Heading 样式为 Markdown 标题
   - 生成 `toc` 目录结构，在 Prompt 中先展示目录再展示全文
   
4. **`initial_state` 传递策略**：
   - 仅在无 checkpoint 时传入 `create_initial_state()`
   - 已有 checkpoint 时传 `None`，让 LangGraph 从 checkpoint 恢复全部状态
   - `auto-generate` 需保留产品信息，但也要合并已有附件
